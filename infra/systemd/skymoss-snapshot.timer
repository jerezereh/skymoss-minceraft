[Unit]
Description=Take a Skymoss world snapshot weekly

[Timer]
# Sunday 04:00, before the nightly restart, so the snapshot captures a settled
# world rather than one mid-restart.
OnCalendar=Sun *-*-* 04:00:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
